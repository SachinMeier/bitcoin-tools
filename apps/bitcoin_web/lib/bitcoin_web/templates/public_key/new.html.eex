<%= render(BitcoinWeb.SharedView, "_subnav.html", subnav_items: [
			%{text: "home", to: Routes.page_path(@conn, :index)},
			%{text: "public key home", to: Routes.public_key_path(@conn, :index)},
		])
	%>

<article class="content">
		<%= render(BitcoinWeb.SharedView, "_header.html", title: @option.title) %>
		<%= render("_#{@option.name}_public_key_form.html", conn: @conn, action: Routes.public_key_path(@conn, :display, option: @option.name)) %>
</article>